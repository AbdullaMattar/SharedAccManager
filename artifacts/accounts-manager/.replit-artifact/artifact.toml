kind = "web"
previewPath = "/"
title = "Shared Accounts Manager"
version = "1.0.0"
id = "artifacts/accounts-manager"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 5173

[services.development]
run = "pnpm --filter @workspace/accounts-manager run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/accounts-manager", "run", "build" ]
publicDir = "artifacts/accounts-manager/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "5173"
BASE_PATH = "/"
